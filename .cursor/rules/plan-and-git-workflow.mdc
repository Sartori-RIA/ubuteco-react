---
description: Update plans before PRs; small focused commits; one plan per PR; pre-PR quality gates (React)
alwaysApply: true
---

# Plan & git workflow (ubuteco-react)

> **Mirror** of [`docs/workflow-plans-and-git.md`](../docs/workflow-plans-and-git.md) — edit the doc first, then sync this file.

Applies when implementing `docs/plans/` in **ubuteco-react**.

## Before opening a PR (mandatory)

### Plan & docs

1. Open the plan doc (`docs/plans/NN-*.md`) and `docs/plans/README.md`.
2. Mark completed phases with `[x]`, set header **`Status: completed`** (or `in progress` if deliberately partial).
3. Include plan doc updates in the **same feature branch / PR** as the code.
4. PR title/body must name the plan and link `docs/plans/...`.

Plan-only edits with **no code** may go direct to **`main`** per AGENTS.md.

### Quality gates (must pass before push / PR)

| Check | Command |
|-------|---------|
| ESLint | `npm run lint` |
| Tests (Vitest) | `npm test` |
| Tests + code coverage | `npm run test:coverage` |
| TypeScript + build | `NEXT_PUBLIC_API_URL=http://localhost:3000 npm run build` |

Run `npm test` locally on TS/TSX changes; run all checks before push/PR.

## Commits: small and focused

One logical change per commit (`docs`, `feat`, `test`, `fix`, `refactor`). One plan → one branch → one PR: `feature/<plan-slug>`.

## PR checklist

- [ ] Plan doc + README status updated
- [ ] Lint, `npm test`, test:coverage, and build green
- [ ] Small commit history
- [ ] Test plan in PR body
- [ ] Companion API PR linked if cross-stack

Full reference: `docs/workflow-plans-and-git.md`.
